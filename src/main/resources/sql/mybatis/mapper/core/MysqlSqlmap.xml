<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.pe.lahuman.service.common.MysqlFunctionDao">
	
	<select id="foundRows" resultType="int">
		<include refid="Core.foundRows"/>
	</select>
	
	<select id="lastInsertId" resultType="int">
		<include refid="Core.lastInsertId"/>
	</select>
</mapper>